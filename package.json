{
  "name": "activa360-cloudflare-d1",
  "version": "1.3.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply activa360-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply activa360-db --remote",
    "db:list": "wrangler d1 list",
    "check": "node scripts/check-project.mjs",
    "db:set-id": "node scripts/set-d1-id.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.44.0"
  }
}
